// Environnement de développement.
// apiBaseUrl vide : les appels restent relatifs et proxy.conf.json les
// redirige vers le backend local (http://localhost:3000).
export const environment = {
  apiBaseUrl: '',
};
